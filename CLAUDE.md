# CLAUDE.md

This repository uses `AGENTS.md` as the shared agent instruction file. Import it
so Claude Code gets the same context as every other agent (project overview,
plans index, architecture notes, skills locations).

@AGENTS.md
